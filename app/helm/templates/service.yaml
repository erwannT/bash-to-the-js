apiVersion: v1
kind: Service
metadata:
  name: {{ include "bash2js-demo.fullname" . }}
  labels:
    {{- include "bash2js-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bash2js-demo.selectorLabels" . | nindent 4 }}
