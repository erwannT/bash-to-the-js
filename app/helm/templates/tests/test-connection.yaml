apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bash2js-demo.fullname" . }}-test-connection"
  labels:
    {{- include "bash2js-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bash2js-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
