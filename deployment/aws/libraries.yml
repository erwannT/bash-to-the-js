s3:
  help: bash2js aws s3 functions
  files:
    - source: "s3.sh"
      target: "%{user_lib_dir}/s3.%{user_ext}"

secret-manager:
  help: bash2js aws secret-manager functions
  files:
    - source: "secret-manager.sh"
      target: "%{user_lib_dir}/secretmanager.%{user_ext}"
